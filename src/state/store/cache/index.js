export const initialState = {
  search: null,
  bestBets: null,
  dictionary: null,
}

export const reducer = (state = initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
}